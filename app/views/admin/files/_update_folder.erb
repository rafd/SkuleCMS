<b> Folder: <%=h @download_folder.name %>, Icon: <%=h @download_folder.icon %></b>
<%= link_to_remote 'Edit', :url => {:action => 'edit_folder', :folder=> @download_folder.id}%>
<%= link_to_remote 'Remove', :url => {:action => 'destroy_folder', :folder=> @download_folder.id}, :confirm => "Are you sure you want to remove this folder?" %>
