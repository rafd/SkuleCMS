-if @club
	%ul#auto_search_results
		-@club.uniq.each do |club|
			%li
				%h4.entry_name.select_tag= club.name
				%span.description= club.description
				%span.tag
					Tag:
					-club.tags.each do |tag|
						=h tag
		%li
			%h5.entry_name
				Search "
				%span.select_tag=@query
				"