<p>
  <b>Name:</b>
  <%=h @download_folder.name %>
</p>

<p>
  <b>Club:</b>
  <%=h @download_folder.club.name %>
</p>

<p>
  <b>Icon:</b>
  <%=h @download_folder.icon %>
</p>

<h2>Listing Downloads</h2>

<table>
  <tr>
    <th>File Name</th>
    <th>Desc</th>
    <th>Url</th>
    <th>Tags</th>
  </tr>

<% @download_folder.downloads.each do |download| %>
  <tr>
    <td><%=h download.name %></td>
    <td><%=h download.desc %></td>
    <td><%=h download.url %></td>
    <td><%=h download.tags %></td>
    <td><%= link_to 'Download', "/data/"+download.url %></td>
    <td><%= link_to 'Edit', edit_club_file_download_path(@download_folder.club, @download_folder, download) %></td>
    <td><%= link_to 'Destroy', club_file_download_path(@download_folder.club, @download_folder, download.id), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add Download', new_club_file_download_path(@club, @download_folder) %> |


<%= link_to 'Edit', edit_club_file_path(@club, @download_folder) %> |
<%= link_to 'Back', :action => 'admin' %>